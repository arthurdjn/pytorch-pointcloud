# ScanNet

Synthetic ScanNet v1 / v2: 5 mesh scenes per split with segments, aggregation and `axisAlignment`, plus the
`processed*/` caches. The label tables under `raw/*/tasks/` are the original ones and are not written by the script.

```bash
bash scripts/generate.sh
```
