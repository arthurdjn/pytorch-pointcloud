# Semantic3D

Synthetic Semantic3D: one labelled train scene and one unlabelled test scene (1024 points each) as `x y z intensity r g
b` rows.

```bash
uv run --no-sync python scripts/generate.py raw ./raw
```
