# ShapeNetPart datamodule. Transforms live in the experiment configs.
_target_: torch_pointcloud.lightning.PointCloudDataModule
batch_size: 32
num_workers: 8

train_dataset:
  _target_: torch_pointcloud.datasets.ShapeNetPart
  root: ${paths.data_dir}
  split: train
  transform: null

val_dataset:
  _target_: torch_pointcloud.datasets.ShapeNetPart
  root: ${paths.data_dir}
  split: test
  transform: null
