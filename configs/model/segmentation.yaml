# Segmentation LightningModule API. `???` args are filled by each experiment; `optimizer` stays null
# for benchmark-only experiments (training raises without one). Optional args: `inferer` (test-time
# strategy), `inverse_key` / `origin_target_key` (voxel-to-raw benchmark scoring).

_target_: torch_pointcloud.lightning.LitSegmentationModel
name: ???
input_keys: ???
pretrained: false
optimizer: null
criterion: ???
target_key: segment
