# Detection LightningModule API. `???` args are filled by each experiment; `criterion` is a `_partial_`
# loss factory completed with the model's head-geometry params; leave it (and `optimizer`) null for
# benchmark-only detectors (training raises). Evaluation decodes raw model output, filters it
# (`min_points`, `score_threshold`), and runs per-class 3D NMS at `nms_iou`.
_target_: torch_pointcloud.lightning.LitDetectionModel
name: ???
input_keys: ???
optimizer: null
criterion: null
