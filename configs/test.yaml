# Eval-only entrypoint: `test.py ckpt_path=path/to/last.ckpt`.
defaults:
  - _self_
  - paths: default
  - hydra: default
  - trainer: default
  - logger: csv
  - data: scannet
  - model: spunet
  - experiment: null

task_name: test
tags: [test]
run_name: default
run_id: ${run_name}_${now:%Y-%m-%d_%H-%M-%S}

seed: 42
ckpt_path: ???
