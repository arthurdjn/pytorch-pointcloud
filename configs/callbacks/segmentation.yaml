# Default callbacks plus per-epoch mIoU and accuracy. Opt in with `override /callbacks: segmentation`.
defaults:
  - default
  - val_miou
  - val_accuracy
  - _self_

# mIoU is the headline metric; keep per-point accuracy off the progress bar.
val_accuracy:
  prog_bar: false
