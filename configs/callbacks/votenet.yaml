# Default callbacks plus per-epoch detection mAP and VoteNet's BatchNorm-momentum decay.
defaults:
  - default
  - val_map
  - bn_momentum_scheduler
  - _self_

# VoteNet selects checkpoints on mAP, not loss.
model_checkpoint:
  monitor: "val/mAP@0.25"
  mode: max
