# @package callbacks.val_ap
# Per-epoch per-class 3D AP, each class matched at its own IoU threshold (the KITTI convention,
# e.g. Car@0.7, Pedestrian/Cyclist@0.5). `iou_per_class` (and usually `class_names`) is
# protocol-specific and set by the experiment.
_target_: torch_pointcloud.lightning.MetricCallback
name: AP
stages: [val, test]
metric:
  _target_: torch_pointcloud.lightning.AveragePrecision3D
  iou_per_class: ???
  class_names: null
