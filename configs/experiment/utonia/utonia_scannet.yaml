# @package _global_
# Training experiment for `utonia-lp.scannet20`. Uses the model's registered transform as a runnable baseline
# input pipeline (matches the architecture's expected features); add dataset augmentation for a
# full from-scratch run. The benchmark counterpart is utonia_segmentation_scannet_benchmark.
# Run: uv run --no-sync python train.py experiment=utonia/utonia_scannet
# Benchmark pretrained: uv run --no-sync python train.py experiment=utonia/utonia_scannet train=false model.model.pretrained=true
defaults:
  - override /data: scannet
  - override /model: segmentation
  - override /callbacks: segmentation

trainer:
  max_epochs: 100
  precision: "32-true" # spconv/torchsparse fp16 tuner fails under 16-mixed

model:
  input_keys: [x, pos, pos_grid, batch]
  model:
    name: utonia-lp.scannet20

data:
  train_dataset:
    transform:
      _target_: torch_pointcloud.models.get_model_transforms
      name: utonia-lp.scannet20
      task: segmentation
  val_dataset:
    transform:
      _target_: torch_pointcloud.models.get_model_transforms
      name: utonia-lp.scannet20
      task: segmentation
