# @package _global_
# PointGPT classification on ModelNet40 (normal-resampled), single pass (no voting).
# Benchmark: uv run --no-sync python test.py experiment=pointgpt/modelnet40
#
# | model.name                         | accuracy |
# | ---------------------------------- | -------- |
# | pointgpt-cguangyan-s.modelnet40    | 0.9331   |
# | pointgpt-cguangyan-b.modelnet40    | 0.9437   |
# | pointgpt-cguangyan-l.modelnet40    | 0.9388   |
# | pointgpt-cguangyan-s.modelnet40-8k | 0.9376   |
# | pointgpt-cguangyan-b.modelnet40-8k | -        |
# | pointgpt-cguangyan-l.modelnet40-8k | -        |
defaults:
  - override /datamodule: modelnet40_normal_resampled
  - override /model: classification
  - override /callbacks: classification

model:
  name: pointgpt-cguangyan-s.modelnet40
  num_classes: 40
  input_keys: [x, pos, batch]
  criterion:
    _target_: torch.nn.CrossEntropyLoss
