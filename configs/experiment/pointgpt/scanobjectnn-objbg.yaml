# @package _global_
# PointGPT classification on ScanObjectNN OBJ_BG (with background).
# Benchmark: uv run --no-sync python test.py experiment=pointgpt/scanobjectnn-objbg
#
# | model.name                              | accuracy |
# | --------------------------------------- | -------- |
# | pointgpt-s.scanobjectnn-objbg.guangyan-chen | 0.9157   |
# | pointgpt-b.scanobjectnn-objbg.guangyan-chen | 0.9707   |
# | pointgpt-l.scanobjectnn-objbg.guangyan-chen | 0.9845   |
defaults:
  - override /datamodule: scanobjectnn
  - override /model: classification
  - override /callbacks: classification

model:
  name: pointgpt-s.scanobjectnn-objbg.guangyan-chen
  num_classes: 15
  input_keys: [x, pos, batch]
  criterion:
    _target_: torch.nn.CrossEntropyLoss

datamodule:
  train_dataset:
    background: true
    variant: null
  val_dataset:
    background: true
    variant: null
